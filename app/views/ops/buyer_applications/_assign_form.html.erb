<%= bootstrap_form_for form, url: assign_ops_buyer_application_path(form.model) do |f| %>
  <%= f.collection_select :assigned_to_id, available_assignees, :id, :email %>

  <%= f.submit 'Assign' %>
<% end %>
