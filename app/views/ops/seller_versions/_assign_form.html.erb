<%= buy_nsw_form_for form, url: assign_ops_seller_application_path(form.model), i18n_scope: 'ops.seller_versions.forms.assign' do |f| %>
  <%= f.input :assigned_to_id, as: :select, collection: available_assignees, label_method: :email, value_method: :id, include_blank: false %>
  <%= f.submit 'Assign' %>
<% end %>
