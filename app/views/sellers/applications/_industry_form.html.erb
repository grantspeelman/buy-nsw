<p>The choices you make here will determine the information you need to complete before submitting your application.</p>

<fieldset class="multiple-choice-group checkbox-group">
  <%= f.collection_check_boxes(:industry, Seller.industry.options, :last, :first, skip_label: true,
                                                                                  wrapper: { class: error_class_for(f, :industry) }) %>
</fieldset>
