<p>Your corporate documents will appear on your seller profile to logged in buyers. These documents help buyers interested in your business to understand it better, without having to ask you for more information. So make sure they are up to date.</p>

<p>Each should be no larger than 5MB and in PDF, PNG or JPEG format. If you have multiple files for a document, please scan and merge as one upload.</p>

<%= render partial: 'file_upload', locals: { f: f, key: :financial_statement } %>
<%= render partial: 'file_upload', locals: { f: f, key: :professional_indemnity_certificate } %>
<%= render partial: 'file_upload', locals: { f: f, key: :workers_compensation_certificate } %>
