<% content_for(:seller_application_class, 'invitations') %>

<%= render_breadcrumbs(seller_application_invitations_breadcrumbs, 'Invite a team member') %>

<h1>Invite a team member</h1>

<p>You can invite other members of your team to your seller profile.</p>
<p>Team members get full access to view and edit your company's profile and application.</p>
<p>We will send an invitation to them by email with further instructions.</p>

<%= bootstrap_form_for form,
                       url: sellers_application_invitations_path(operation[:application_model]),
                       as: :invitation do |f| %>

  <%= user_error_messages_for_model(operation['model']) %>

  <%= f.text_field :email %>
  <%= f.submit 'Send invitation' %>
<% end %>
