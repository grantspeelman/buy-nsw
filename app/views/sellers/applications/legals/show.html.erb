<% content_for(:step_container_header) do %>
  <h1>Your legals</h1>
  <p>You'll need to complete the disclosures and accept the Core Terms to become a seller.</p>
<% end %>

<%= render partial: 'sellers/applications/shared/step', locals: {
  form: form,
  form_path: legals_sellers_application_path(application),
  application: operation[:application_model],
  operation: operation,
  hide_header: true,
} %>
