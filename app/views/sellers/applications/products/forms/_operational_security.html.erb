<fieldset class="multiple-choice-group radio-group">
  <%= f.input :change_management_processes, as: :radio_buttons, wrapper: false, collection: Product.change_management_processes.options %>
</fieldset>

<fieldset>
  <%= f.input :change_management_approach, as: :text, input_html: { rows: 5 } %>
</fieldset>

<fieldset class="multiple-choice-group radio-group">
  <%= f.input :vulnerability_processes, as: :radio_buttons, wrapper: false, collection: Product.vulnerability_processes.options %>
</fieldset>

<fieldset>
  <%= f.input :vulnerability_approach, as: :text, input_html: { rows: 5 } %>
</fieldset>

<fieldset class="multiple-choice-group radio-group">
  <%= f.input :protective_monitoring_processes, as: :radio_buttons, wrapper: false, collection: Product.protective_monitoring_processes.options %>
</fieldset>

<fieldset>
  <%= f.input :protective_monitoring_approach, as: :text, input_html: { rows: 5 } %>
</fieldset>

<fieldset class="multiple-choice-group radio-group">
  <%= f.input :incident_management_processes, as: :radio_buttons, wrapper: false, collection: Product.incident_management_processes.options %>
</fieldset>

<fieldset>
  <%= f.input :incident_management_approach, as: :text, input_html: { rows: 5 } %>
</fieldset>

<fieldset class="multiple-choice-group radio-group">
  <%= f.input :access_control_testing_frequency, as: :radio_buttons, wrapper: false, collection: Product.access_control_testing_frequency.options %>
</fieldset>
