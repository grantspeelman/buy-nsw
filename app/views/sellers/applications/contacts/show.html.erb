<% content_for(:step_container_header) do %>
  <h1>Your contact details</h1>
  <p>The information here is available to all buyers.</p>
<% end %>

<%= render partial: 'sellers/applications/shared/step', locals: {
  form: form,
  form_path: contacts_sellers_application_path(application),
  application: operation[:application_model],
  operation: operation,
  hide_header: true,
} %>
