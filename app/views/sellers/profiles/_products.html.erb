<% if seller.products.any? %>
<ul id="products">
  <h2>Products</h2>
  <% seller.products.each do |product| %>
  <li>
    <%= link_to product.name, pathway_product_path(product.section, product) %> &mdash;
    <%= product.summary %>
  </li>
  <% end %>
</ul>
<% end %>
